class MyClass {
    int value;

    public MyClass(int value) {
        this.value = value;
    }

    public void someMethod() {
        // Method implementation
    }
}

public class Main {
    public static void main(String[] args) {
        MyClass[] objects = new MyClass[5]; // Creating an array of MyClass objects

        // Initializing and assigning values to objects
        objects[0] = new MyClass(10);
        objects[1] = new MyClass(20);
        objects[2] = new MyClass(30);
        objects[3] = new MyClass(40);
        objects[4] = new MyClass(50);

        // Printing the elements of the array
        for (int i = 0; i < objects.length; i++) {
            System.out.println(objects[i].value);
        }
    }
}
